{
  "name": "tests",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "keywords": [],
  "author": "",
  "license": "ISC",
  "scripts": {
    "start": "docker compose up -d; pnpm run playwright:test",
    "stop": "docker compose down",
    "test": "docker compose build && docker compose up -d && pnpm run playwright:test; docker compose down",
    "test:ui": "docker compose build && docker compose up -d && pnpm run playwright:test --ui; docker compose down",
    "test:headed": "docker compose up -d && pnpm run playwright:test --headed && docker compose down",
    "playwright:test": "npx playwright test",
    "taze:major": "taze major -wIr",
    "taze:minor": "taze minor -wIr"
  },
  "devDependencies": {
    "@nuxt/test-utils": "^3.14.4",
    "@playwright/test": "^1.48.2",
    "@types/node": "^20.17.6",
    "taze": "^0.18.0"
  },
  "dependencies": {
    "pocketbase": "^0.21.5"
  }
}
